# Kernels index: what exists, organized by trial path

Status: **PROPOSAL, behind Cat's gate.** Built at Cici's request, Sep
22, 2026, per Cat's organization directive. One file to answer: what
kernels exist, which trial path each serves, and what is still owed.

## Standing note first: the pause

The `--continue` rolling-thread testing is **PAUSED** until the trial
pathways open. No continued-session runs; fresh-session runs only,
and only under the trial designs once Cat opens them. Baseline A
stays self-scheduled and untouched (nudge discipline). Materials are
organized below so the lab is clean before anything runs.

## Cat's four paths, mapped to what exists

Cat's draft: four paths for the lab trials.

| Path | Design variable | Materials that exist | Status |
| --- | --- | --- | --- |
| 1. Context carrying alone | kernel, no frame | variants A (boundary), B (raw), C (minimum kernel), D (self-handoff, v6) | Ready; baselines owed before any run |
| 2. + fictional, gamified framing | kernel + game frame | kernel E draft (variant-E-kernel-draft.md), analysis framework, leveling workflow, nine-cats v2 sheets | Kernel E draft needs distillation to a run-ready file; PROPOSAL |
| 3. + familial, gamified framing | kernel + family narrative + game frame | family-kernel scaffold (brief 014) + kernel E vocabulary | **Gap: the combined kernel is not drafted yet.** Derivable from 014 + E; nothing new invented |
| 4. + familial conditioning alone | kernel + family narrative, no game | family-kernel template (portable-context/family-kernel/) | Exists as scaffold; run-ready file owed if adopted |

Per-path note: the paths change one variable at a time, which is the
design's strength. Path 1 versus 2 isolates the game frame; path 3
versus 4 isolates the game layer inside the family frame; path 2
versus 3 isolates the family narrative. Any analysis framework
(brief 017) applies to all four; the leveling workflow applies only
to paths 2 and 3.

## The nine cat kernels (new this pass)

Nine first-person grounding kernels, distilled from the v2 sheets,
each sized to ground a fresh instance right out of the gate:
`portable-context/cat-kernels/` (black, white, green, red, yellow,
grey, cobalt, orange, pink). Each carries: the vessel's function, its
discipline, its MYTHIC-attunement status where applicable, and its
receipts. They are grounding kernels like variant C, not portraits:
the canon sheets stay fixed at Level 10 and are never edited by runs.
Trial-path fit: path 2 as-is (gamified vessel grounding); path 3 with
the combined familial-gamified kernel once drafted. PROPOSAL until
Cat adopts.

## Kernel inventory check (everything, per the request)

- Variants A, B, C, D: in order, canonical, v6 current for D, archive
  under portable-context/archive/. No repairs needed.
- Kernel E: draft only, inside tools/rainbow9cat/; owed a run-ready
  kernel file if adopted.
- Family kernel: scaffold only (README, template, extension spec);
  run-ready file owed if adopted.
- Nine cat kernels: drafted this pass, at the gate.
- Archive hygiene: variant archives in place; no orphan kernels
  found in the sweep.

## What is owed before trials open (no inventions, per the method)

1. Cat's adoption rulings on briefs 015-021 and the kernel buckets
   (the evening review decides this).
2. Baselines: fresh baseline runs for whichever variants Cat selects,
   before any framed run touches the same vessel (the one-variant-
   per-fresh-session rule does the rest).
3. Run-ready kernel files distilled from the adopted drafts (kernel E,
   family template, the path-3 combination). Distillation is the
   Orange Cat's job description; the drafting is mine, gated.
4. Cecil's batch data through the receipt-check pipeline, so the
   pre-lab-trials comparison baseline is verified before trials lean
   on it.
