# Lab project logs

Running per-person logs, created at Cat's direction at the Sep 22
evening gate close (all 31 open questions ruled). Each log carries the
open threads that person is holding, so work continues from the record
instead of from memory. Append-only per person; the orchestrator's log
(Cat) is the top-level view. Rulings are recorded in
notes/open-questions-for-the-gate.md and the queue (brief 006).
