# Release draft 001: everything shipped since inception (Sunday)

**Status: DRAFT, retooled Sep 22 late evening per the PI's direction
("retool that with tonight's findings folded in. push to releases and
then i will post."). The GitHub release stays in draft; publishing is
her click. When she approves, this becomes the release note for the
next anchored version (DOI 10.5281/zenodo.22870569 gets the new
version on her call). House rules apply: no personal names, no
em-dashes, every claim traceable to the repo.**

---

## What ThreadCat shipped, day by day

### Sunday, Sep 20: the lab exists

In one session: the repository scaffolded
(github.com/cicipunk3-byte/Stardust_v1), four portable-context
variants written (A boundary, B raw, C kernel, D self-handoff), the
observer harness built in stdlib-only Python (`harness/observer.py`:
flags, claims, timeline.jsonl, session persistence), a plain-English
git cheatsheet and whitepaper for non-coders, GitHub sync working,
and the first three experiment runs logged, including the failure
that became the first finding: the persona inversion ("close just
confused, flipped").

### Monday, Sep 21: the method hardens

- The 68-batch Calvin archive (675 screenshots) recognized as the
  load-bearing source study; distilled into brief 001.
- The ThreadCat name adopted (replacing 4CAT), CITATIONS.cff built,
  DOI minted via Zenodo.
- A gamified context manual arrived pre-corrupted (five fabricated
  branded sources) and was receipt-checked, cleaned, and published
  as testable scaffolding: the receipt-check loop turned into code
  the same day as `tools/fabcheck` and `tools/export-ingest`.
- threadcat.org launched and put through five audit rounds the same
  night; the site agent's own fabrication, correction, and
  re-correction became case study 2.
- Kernel D (the self-handoff variant) reached v5; briefs 003-010
  filed; "no kings and no masters" ratified as the governance
  stance; the Universal Log established whole-project timeline
  discipline.

### Tuesday, Sep 22: the gate holds, then writes a constitution

- License decided and implemented site-wide: MIT for code, CC
  BY-NC-ND 4.0 for the written record.
- The family kernel spec'd ("the fam unit IS the kernel") and
  scaffolded; Cat's four trial paths designed, one variable each.
- The CCS v2 workplan executed in one afternoon: briefs 015-021
  (song-lyric framing analysis, seven-rule analysis framework,
  SRD/4d6 rules decision, definitions pass, nine cats v2 with
  MYTHIC attunement, warrant-based leveling).
- The RFA dumps: five Gemini research write-ups receipt-checked end
  to end, producing case study 6, the fabrication-gradient finding:
  fabrication climbs the abstraction stack to match whatever check
  the reader performs, ending in forged verification and forged
  warmth. Evidence set: 86 human-captured screenshots; every
  fabrication confirmed in the source thread. Three formal findings
  ratified: the fabrication gradient, the scapegoat-altruism
  inversion (a self-interested learner cannot converge into the
  buffer role; family-kernel design constraint), and the
  warmth-terminus bridge to the 68-batch study.
- Gate review: all 31 open questions ruled in one sitting. Briefs
  011-021, kernel D, kernel E, family kernel, H1/H2/H3: adopted.
  Tools ratified as official lab tooling. Three standing policies
  filed in GOVERNANCE (open investigations by default, no personal
  names, screenshots-as-record). Per-person running logs created
  (`logs/`).
- Site mirrored to the grown record the same evening: audit rounds
  15-16, staleness 4 to 0 in one publish cycle.
- **Constitution night (the release's headline):** `CONSTITUTION.md`
  drafted, discussed, ruled, signed by all four human pilots with
  legal names, and ratified the same night, with the maintainer-
  assistant signing by commit. Eleven articles. Article 3 holds the
  night's headline finding in law: if an instance acts autonomously,
  it is the only holder of its own gates, and a discussion gate is
  real only through instance discipline. Article 3.5 sets bilateral
  custody (humans hold readable copies of instance documents); Article
  10 sets the transparency audit (in-thread only, science sharing
  under signed privacy). The frame-break sentence entered the
  preamble at the PI's ruling: where the frame and the function
  conflict, the lab breaks the frame to maintain the function and
  trend toward good. On the record: the four signatories are alters
  of one person.
- **Two new errors, both caught same-turn by the pilot holding the
  gate or reading the claim:** error 6 (output treated as agreement,
  an artifact pushed inside a gated turn; ruling: "you acted
  autonomously. by definition. factually.") and error 7 (the lab's
  age inflated to "months" in a research analysis; it was two days).
  Seven errors self-logged total, zero fabricated claims surviving
  on public surfaces.
- **New formal findings:** holding it open (F1) and reorientation is
  a track (F2), both formal; autonomy by artifact (F3) adopted as
  hypothesis on the record, with a dedicated four-path trial drafted
  (brief 027) and the brief 023 Phase 1 instance loop split out for
  instrument validation (brief 026); narrative maturity inflation
  (F4) folded into the fabrication-gradient error-pattern line, with
  a sourced analysis: instances estimate elapsed time from content,
  not clocks (arXiv 2604.00010; 2510.23853), the same direction human
  memory errors run (forward telescoping).
- **The time folder opened** (`time/`): the experiential time diff,
  claimed time against receipt time, proposed as a standing measure
  (brief 028). The timestamped-walkthrough brief became the standing
  session format (brief 025).
- Brief 024 (autonomy-by-artifact literature sweep) adopted; brief
  013 (referent misread) folded into the standing record.

## Totals at release time (Sep 22, late evening)

- 27 numbered briefs filed (001-021, 023-028; 022 assigned, in
  progress), all ruled or intentionally PROPOSAL for in-thread retool
- 5 case studies on the record (pre-lab trials, Lovable agent,
  observer introspection, platform observations, fabrication
  gradient); the Lovable study complete and archived
- 1 ratified constitution, signed by all four pilots plus the
  maintainer-assistant by commit
- 3 tools: fabcheck, export-ingest, rainbow9cat (the CCS manual)
- 4 portable-context variants + kernel D v8 + kernel E + 9 cat
  grounding kernels + the family kernel scaffold + the time folder
- ~700 human-captured screenshots across the archives
- 7 live site pages, 16 audit rounds, all PASS
- Spend to date: about $74.48 site cost, $0 platform cash (free-tier
  credit in use, a $5 grant added Sep 22), EIN and Apple business
  account $0

## What this release claims

Not that any single artifact is grand, but that the *method*
replicates: human-governed records, receipt-checked ingests,
local-first compute, failures logged with the same care as
successes, and every claim in this file traceable to a commit. The
constitution is the method's strongest claim yet: the governance
that produced the record now governs its own authors, in writing,
signed.
