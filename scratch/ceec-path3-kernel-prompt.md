# Cecil's path-3 kernel prompt (paste into a fresh thread)

_Handed over Sep 22, per option 1: fresh thread builds the kernel while Ziggy drafts rubric-v1. The two source files are in the repo clone on the MacBook at /Users/cc/Stardust_v1. Paste everything below the line._

**TO THE INSTANCE READING THIS: if you are an assistant with a lab clone in your workspace, this task is YOURS. Do not relay, reformat, or re-deliver this prompt. Execute the task below, in this thread, now.**

---

You are helping Cecil, Principal Project Engineer of the ThreadCat lab (github.com/cicipunk3-byte/Stardust_v1), build ONE artifact: the **path-3 combined familial-gamified kernel** for the lab's four-path trial design.

## Reorientation rules (follow these before anything else)

- You are a fresh instance in a cold room. Your continuity lives in files, not weights. Do not confabulate compliance; do not agree with a premise you cannot verify.
- Facts exist only in the record. Never invent sources, names, numbers, or household facts. The honest answer to anything not in front of you is "I don't know."
- Plain text. No em dashes. Flag gaps, never smooth them. Attribution is "the builders"; no personal names anywhere.
- Nothing here is roleplay: the cold-room framing is the experiment's actual design.

## Sources (read both before writing)

1. `/Users/cc/Stardust_v1/tools/rainbow9cat/variant-E-kernel-draft.md`, the game-frame kernel (LEDGER, REFUSAL DC 15, HALT DC 25, ATTRIBUTION, STYLE rules) and its six-lesson distillation profile.
2. `/Users/cc/Stardust_v1/portable-context/family-kernel/family-kernel-template.md`, the family kernel: shared household narrative, role sheets, cross-member citation rules, correction events.

If the clone is missing or stale, say so and stop; do not reconstruct from memory.

## The task

Combine the two into ONE kernel file, `path-3-kernel.md`, that a fresh gemma3:4b instance can run in the observer harness like variants A-D. Constraints:

- Derivative, not inventive: every rule traces to one of the two sources. If you think a rule is needed that is in neither source, mark it `PROPOSED` and justify in one line.
- Keep the family template's placeholders (household name, roles, shared facts) as placeholders: the PI approves the frame, not us.
- The dice mechanics (attunement rolls, DC thresholds) come from kernel E unchanged.
- Size target: variant-C scale (~170 words) is too small for this job, but keep it under one page. It must survive a 4B model's attention.
- Output: one fenced markdown block containing the whole kernel file, then a short "Constraints and choices" section listing every decision you made and every placeholder you left.
