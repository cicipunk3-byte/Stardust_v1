# nextcheck; claim queue with mandatory run-the-artifact step (Yellow Cat)

Part of the ThreadCat lab tooling. Free, local, stdlib-only Python.

## Status: PENDING PI ADOPTION; fixture tests pass

Fixture tests pass (4/4). Web verification stays an honest stub; run-the-artifact step mandatory in output.

The brief 022 design, scaffolded. Extracts claim-shaped sentences from a
document into a NEEDS-HUMAN queue; web verification is an honest stub
because it costs money; the run-the-artifact line item is mandatory in
the output so a claim queue can never masquerade as verification.
Fixture tests against a known-fabricated doc are owed.
