# Archive: the test pass of September 23, 2026

Prior versions of every file modified by the comprehensive test pass,
restored from git at commit 74adbe5 (the scaffold push) per the PI's
same-day ruling that priors get archived, not just left to history.

Contents, mirroring the repo paths:

- `briefs/` side: brief 042's pre-test-pass body is archived at
  `briefs/archive/042-v1-2026-09-23-pre-test-pass.md` (house brief-archive convention).

This directory:

- the nine tool READMEs as first pushed (SCAFFOLD, PENDING TESTING banners)
- the three tool cores as first pushed, with the bugs the fixture tests caught:
  - cleanroom: counted run_id as carried text; phantom +1 token artifact
  - kernelpress: counted re.split's leading empty chunk as a section
  - nextcheck: queued chat filler via past-tense claim hints

Why keep the bugs: they are the benchmark baseline. The fixtures that
caught them are the regression controls; the broken priors show what the
signals look like when a detector is miscalibrated. All data and testing
is useful for the public, save for that which contains private data.
These are tools; none of these files contain private data.
